import React from 'react';

export default () => (
  <ol>
    <li> Lines 8-10: configure default / global properties using the &lt;DFPSlotProvider&gt;.</li>
    <li> Line 13: define an ad-slot using the &lt;AdSlot&gt; component.</li>
    <li> Line 16-20: define a second ad-slot with custom targeting argument customKw2='hello'.</li>
  </ol>
);
