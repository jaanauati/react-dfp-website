import React from 'react';

export default () => (
  <ol>
    <li>
      Lines 8: configure a global dfpNetworkId via &lt;DFPSlotProvider&gt;.
    </li>
    <li>
      Line 10: we define another ad slot using the &lt;AdSlot&gt; component.
    </li>
    <li>
      Line 13: we define an ad-slot using the &lt;AdSlot&gt; component.
      In this case, we configure the slot to render out-of-page using the
      property: &apos;renderOutOfThePage&apos;.
    </li>
  </ol>
);
