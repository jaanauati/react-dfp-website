import React from 'react';

export default () => (
  <ol>
    <li>
      Lines 8: configure a global dfpNetworkId via <strong>&lt;DFPSlotProvider&gt;</strong>.
    </li>
    <li>
      Line 10: we define another ad slot using the <strong>&lt;AdSlot&gt;</strong> component.
    </li>
    <li>
      Line 13: we define an ad-slot using the <strong>&lt;AdSlot&gt;</strong> component.
      In this case, we configure the slot to render out-of-page using the
      property: &apos;renderOutOfThePage&apos;.
    </li>
  </ol>
);
